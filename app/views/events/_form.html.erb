<div id="new_event_form">
<%= simple_form_for @event, :remote => true do |f| %>
  <%= f.error_messages %>
  <%= f.input :title, :label => 'Event' %>
  
  <div class="input string required">
    <label class="string required" for="event_start_date">
      Start date
    </label>
    <input class="string required" id="event_start_date" 
    name="event[start_date]" size="50" type="text"
    value="<%= @event.start_date.year if @event.start_date %>" />
  </div>

  <div class="input string required">
    <label class="string required" for="event_end_date">
      End date
    </label>
    <input class="string required" id="event_end_date"
    name="event[end_date]" size="50" type="text"
    value="<%= @event.end_date.year if @event.end_date%>" />
  </div>

  <%= f.hidden_field :timeline_chart_id, :value => @timeline_chart.id %>
  <%= f.submit %>
<% end %>
</div>
